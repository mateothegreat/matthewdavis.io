---
import Layout from "@/layouts/Layout.astro";

const childPages = [
  { slug: "support-slug-01", title: "Support 01" },
  { slug: "support-slug-02", title: "Support 02" },
  { slug: "support-slug-03", title: "Support 03" }
];
---

<Layout>
  <div class="container mx-auto px-4 py-8">
    <h1 class="mb-6 text-2xl font-bold">Support hub page</h1>
    <ul class="list-inside list-disc space-y-2">
      {
        childPages.map(({ slug, title }) => (
          <li>
            <a
              href={`support/${slug}`}
              class="text-blue-600 hover:underline">
              {title}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</Layout>
