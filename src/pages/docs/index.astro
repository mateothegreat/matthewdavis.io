---
import Layout from "@/layouts/Layout.astro";

const childPages = [
  { slug: "docs-slug-01", title: "Docs 01" },
  { slug: "docs-slug-02", title: "Docs 02" },
  { slug: "docs-slug-03", title: "Docs 03" }
];
---

<Layout>
  <div class="container mx-auto px-4 py-8">
    <h1 class="mb-6 text-2xl font-bold">Docs hub page</h1>
    <ul class="list-inside list-disc space-y-2">
      {
        childPages.map(({ slug, title }) => (
          <li>
            <a
              href={`docs/${slug}`}
              class="text-blue-600 hover:underline">
              {title}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</Layout>
