---
import Layout from "@/layouts/Layout.astro";

const childPages = [
  { slug: "about-slug-01", title: "About 01" },
  { slug: "about-slug-02", title: "About 02" },
  { slug: "about-slug-03", title: "About 03" }
];
---

<Layout>
  <div class="container mx-auto px-4 py-8">
    <h1 class="mb-6 text-2xl font-bold">About hub page</h1>
    <ul class="list-inside list-disc space-y-2">
      {
        childPages.map(({ slug, title }) => (
          <li>
            <a
              href={`about/${slug}`}
              class="text-blue-600 hover:underline">
              {title}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</Layout>
